name: report-automation

on:
  pull_request:
    types: 
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - run: |
          python ./${{ github.workspace }}/report-automation.py          